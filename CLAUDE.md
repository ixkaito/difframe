# Difframe

参考サイトや Figma プロトタイプを **iframe**（画像ではなく実サイト）として実装ページの上に重ねて表示する、ピクセルパーフェクト比較用のブラウザ拡張機能（Chrome/Firefox 両対応、Manifest V3）。PerfectPixel の iframe 版というコンセプト。

開発は Firefox を主戦場とし、Chrome は後追いで確認する運用。自動テストは無く、検証は常に「拡張機能を再読み込みして目視確認（スクリーンショット添付でのフィードバックも多い）」。

## ファイル構成

- `manifest.json` — MV3。Chrome/Firefox 両対応のため `background.service_worker` と `background.scripts` を併記
- `background.js` — declarativeNetRequest によるヘッダ剥がし（X-Frame-Options/CSP 除去）
- `content.js` / `content.css` — オーバーレイ本体。`all_frames: true` でオーバーレイ iframe 自身にも注入される
- `popup.html` / `popup.js` / `popup.css` — 設定 UI（プリセット管理など）
- `icons/` — アイコン。SVG 原本（`icon.svg`=黒、`icon-light.svg`=白）から `sips -s format png -z <size> <size>` で各サイズの PNG を生成。黒はライトテーマ用、白はダークテーマ用（Firefox の `theme_icons`）
- `_locales/en|ja/` — i18n（`default_locale` は en）。popup.html は `data-i18n` / `data-i18n-title` 属性を popup.js 起動時に一括置換、JS 内の動的文言は `chrome.i18n.getMessage`。言語はブラウザの UI 言語で自動選択

## 主な機能

- **プリセット管理**: URL・不透明度・拡大率・幅・高さ・X/Y位置・ブレンドモードを保持し、適用範囲「タブ / ページ / ホスト」のいずれかに割り当てる（デフォルトはタブ、UI上も先頭）
- **ブレンドモード「差分」**: `mix-blend-mode: difference` で一致部分が黒に消え、ズレだけが浮き出る
- **操作対象の切り替え**（「ページ」/「オーバーレイ」2択）: クリック・スクロールの届き先を明示的に切替
- **スクロール連動**（相対差分ベース、デフォルトON）
- **適用範囲「タブ」**（タブローカルのプリセット上書き）: `sessionStorage` 利用（タブ×origin 単位、リロードで維持、タブを閉じると消える）。同じページを複数タブで開き PC用/SP用を並行比較できる
- **画像貼付フォールバック**: iframe埋め込み拒否サイト向けに、画像（⌘V貼付 or ページ上ドロップゾーン）をオーバーレイにできるモード
- Figma共有リンクをそのまま貼れる（embed URL変換）

## 設計判断と理由

- **DNRヘッダ剥がしはタブ限定の session rule**: 当初グローバルなdynamic ruleだったが、1タブでONにするだけで全タブのCSP/XFOが弱まるセキュリティ問題があったため `tabIds` 条件付きに変更。タブを閉じたら自動削除。初回有効化時はルール適用完了を待ってからiframeを読み込む（レースコンディション対策）。
- **操作対象は「ページ/オーバーレイ」の2択に整理**: 当初「ロック」+「移動モード」の2チェックボックスで3状態を表現していたがわかりにくく、明示的な3択セグメント→さらに「移動モード」を撤廃（数値入力＋矢印キーの1px単位調整で代替可能）し2択に簡素化。
- **適用範囲は「タブ/ページ/ホスト」の3択セグメント**: 当初「ページ別/ホスト別」2択＋「このタブのみに適用」チェックボックスだったが、タブ上書き中はスコープ選択が無意味（無意味な組み合わせが表現できる）なため3択に統合。デフォルトはタブ（UI上も先頭）。セグメント下に適用キーを常時表示し、タブは「このタブのみ（host）」表記で origin 境界（sessionStorage はタブ×origin 単位）を明示。
- **グローバルなスコープ設定は持たない（スコープレス解決、dfStore v4）**: 当初の `settings.scope` は全サイト・全タブ共有で、サイトBでスコープを切り替えるとサイトAの割り当てが突然解決されなくなる問題があった。設定自体を廃止し、解決を常に タブ上書き（sessionStorage）→ host+path → host のフォールバックに変更。セグメントは「有効な割り当ての保存場所」の投影で、切替は割り当ての移動操作（タブへ＝内容を引き継いで上書き開始・共有側は残す、ホストへ＝shadow する path 割り当てを削除して移動、ページへ＝コピーで host 側は残す）。狭いスコープが広いスコープを shadow するため、OFF の path 割り当てで「ホスト全体ON・このページだけOFF」も表現できる。タブ上書きは操作時に遅延作成（ページ読み込み時に空上書きを作ると共有割り当てへの追従を妨げる）、未割当時のスコープ選択は非永続の `pendingScope` が次の割り当ての行き先だけ覚える。
- **スクロール連鎖対策はiframe内から解決**: クロスオリジンiframeに親から`overscroll-behavior`は効かせられないが、拡張機能なのでiframe内にもcontent scriptを注入できる、という発想でoverscroll-behaviorをiframe内に注入して解決。「ページ」モード中のオーバーレイスクロールは`overflow:hidden`の付け外し（レイアウト幅が変わる副作用があった）ではなく、常時スクロールバー表示のままwheelを`preventDefault`する方式。
- **`mix-blend-mode: difference` は単一要素に統合**: blendはスタッキングコンテキストの境界（html直下）にある要素、かつサイズ・transform・opacityを担う要素と**同一**でないと効かない。入れ子構造（root > frame）だと合成されないため、サイズ・transform・opacity・blend・背景を全て`#df-overlay-root`単体が持つ構造に統合し、スクロール遮断用の透明レイヤーは兄弟要素`#df-shield`に分離。また明示的な背景色が無いページはキャンバス色が合成グループ外になるため、背景が無い時だけ白背景を強制。
- **Radix UIはコンポーネントではなくトークンだけ移植**: Radix Colors（Slate/Indigo/Red）・スペーシング・radius・コントロール高さ（size2=32px）等をCSS変数として移植。アイコンもRadix IconsのSVGをインライン埋め込み。外部依存ゼロを維持する方針。
- **タブローカル上書きに`sessionStorage`を採用**: タブごとに独立・リロードで維持・タブを閉じれば消える性質がそのまま要件に合致。共有バインディングは`chrome.storage.local`のまま。
- **画像データは別ストレージキー（`dfImages`）に分離**: 画像データURLが大きくなる前提で、設定オブジェクト本体を毎回書き直さずに済むように。`unlimitedStorage`権限を付与。
- **ポップアップを開いた初回だけストアを読み直す**: 各タブの content script が `store` 全体を持って丸ごと書き戻す構造なので、`storage.onChanged` を取り逃したタブ（bfcache 復帰など）が編集すると他タブの変更を消してしまう。編集が始まる直前＝ポップアップの初回 `getData` でのみ `load()` してから応答する（以後の `refresh()` は編集のたびに走るので読み直さない。スライダー操作のような連続経路を重くしないため）。根本解決＝単一writer化は保留。
- **ヘッダ剥がしはタブの「今のページ」に追従させる**: セッションルールはタブ単位なので、遷移先の content script が消すのを待つと遷移先のサブフレームまで無防備になり、content script が動かないページ（PDFビューア等）ではタブを閉じるまで残る。background の `tabs.onUpdated`（`status === "loading"`）で遷移開始時に削除し、必要なら遷移先が改めて有効化する。また実際に iframe を読む時だけ剥がす（画像モード・URL未入力では剥がさない）。
- **SPA遷移の検知は background 経由**: 割り当ての解決キーに `pathname` を使うため同一ドキュメント遷移でも再解決が要るが、content script は分離ワールドなので `history.pushState` を書き換えてもページ側の呼び出しは捕まらない。`tabs.onUpdated` の「`status` を伴わない `url` 変化」を同一ドキュメント遷移とみなして content script に通知する（`webNavigation` 権限は「閲覧履歴の読み取り」警告が増えるので使わない）。このときスクロール基準は取り直さない: 遷移後にフレームワークがトップへ戻す分は相対同期が自然に打ち消すので、ここで rebase すると逆にその戻り分だけズレる。
- **ポップアップはファイル選択/D&Dをホストできない**: フォーカスを失うとポップアップが閉じる仕様のため、画像選択UIはページ側（content script）にフルスクリーンのドロップゾーンとして表示。⌘V貼付だけはポップアップ内で完結。
- **画像モードの「覗き窓」**: 幅・高さ・X/Y・拡大率は`<img>`側に適用し、`#df-overlay-root`は`100vw`/`100vh`のビューポート全面スクロールコンテナ（`inset:0`ではなくこちら）。`100vw`がページ自身のスクロールバー幅を含むため、rootのスクロールバーが可視領域外に自然にはみ出す（＝iframeでスクロールバーが見えないのと同じ原理）ことを意図的に利用し、CSSで隠す処理を不要にしている。
- **URL確定はEnter/「適用」ボタンのみ**（blurでは確定しない）: フォーカスが外れただけで入力途中のURLが保存・読み込みされる事故を防ぐため。
- **ポップアップの高さは約600pxが上限（仕様）**: ブラウザ側でハードキャップされておりCSSでは解決不可。サイドバー化（Firefox専用API）は見送り、UIのコンパクト化で対応する方針。

## 過去に判明した非自明なバグの原因（再発防止用）

- 縦横スクロールバーの連鎖: `body`に固定幅を指定していたのが原因（`html`側に移して解決）
- ヘッダーの縦ズレ: グローバル`label { margin-bottom: 5px }`をトグル用labelが継承していたのが原因（`top: -1.5px`の光学補正ハックは誤診断だった。表面対処ではなく`.toggle`に`margin:0`を当てて根本解決）
- Chromeでスライダー進捗が塗られない: `::-moz-range-progress`はFirefox専用。WebKitは`linear-gradient`+CSS変数`--fill`で塗る必要がある
- 画像モードでスクロールが頭打ち: `#df-overlay-root`側に幅/高さを与えていたため。幅/高さ/拡大率は`<img>`側、rootはビューポート全面という構造に変更して解決
- `typeof browser`でのFirefox判定が誤動作: Chrome 148+は`browser`名前空間を標準サポートするため常にtrueになる。Firefox専用API（`browser.runtime.getBrowserInfo`）の有無で判定すること

## 保留中・未着手

- **レスポンシブ自動切り替え**（PC/SP用2スロットを幅で自動選択、案A/案Bまで検討済み）: タブローカル上書きで代替できる場面が増えたため優先度低で据え置き
- **Basic認証が必要な参照ページ**: DNRで`Authorization`ヘッダをタブ限定注入する設計は可能（ヘッダ剥がしの仕組みを流用）だが未実装。ブラウザ側で認証済みなら現状で足りるため保留
- **オーバーレイON時のフェード/トランジションの原因不明の挙動**: 一度は全称セレクタ漏れを疑い防御CSSを入れたが別問題と判明しロールバック。原因未特定
- **スクロールバーの「レイヤー」切り替え**（操作対象によってどちらのバーを見せるか）: ネイティブでは不可能と結論、自前インジケータ案は未実装のまま
- **`label`のグローバルmargin指定のスコープ整理**: 複数回話題に出たが未着手
- **`dfStore`の単一writer化 or 保存単位の分割**: 各タブが `store` 全体を持って丸ごと書き戻すため、古いスナップショットからの書き込みが他タブの変更を消し得る。ポップアップ初回の読み直しで実用上は塞いだが、background を唯一のwriterにするかキーを分割するのが本筋
- **公開準備の残り**（README・LICENSE(MIT)・拡張機能アイコン・gecko.id=`@difframe`・v1.0.0・提出用zipは対応済み。残りはストアでの提出作業のみ）:
  - 提出用 zip は `zip -q dist/difframe-<ver>.zip manifest.json background.js content.js content.css popup.html popup.js popup.css icons/*.png _locales/*/messages.json` で生成（dist/ は gitignore 済み）。gecko.id は AMO 初回提出で永久確定する（Mozilla 自身の拡張と同じ `@name` 形式。提出前なら変更可）
  - **ストア掲載素材（未着手）**:
    - スクリーンショット: 1280×800 で作れば両ストア共用（CWS は必須1〜5枚・JPEG/24bit PNG・透過不可、AMO は任意・推奨1280×800・1.6:1）。構成案: ①コンセプト全景（実装ページ+半透明オーバーレイ+ポップアップ） ②差分モードでズレだけが浮かぶ画面 ③ポップアップUIのアップ ④Figma共有リンク ⑤タブスコープでPC/SP並行比較。CWS はロケール別に設定できるので en/ja の2セット。macOS の Chrome は `--lang` を無視するため英語UIは `defaults write com.google.Chrome AppleLanguages '(en)'` で切替（戻すのは `defaults delete com.google.Chrome AppleLanguages`）。Firefox は設定→一般→言語。Retina だと2倍pxで撮れるので 2560×1600 で撮って縮小が楽
    - CWS ストアアイコン: ガイドラインは絵柄96×96+各辺16px透過余白・ライト/ダーク両背景で視認可。現 `icon-128.png` は余白8px・純黒なので、提出用に余白広めの版（必要なら中間色版）を SVG から `sips` で別途生成する
    - AMO アイコン: 32×32 / 64×64 を SVG から生成して Developer Hub にアップロード
    - CWS スモールプロモタイル 440×280 は必須（文字は避け、半分に縮小しても成立する絵）。マーキー 1400×560 は任意（フィーチャー掲載の対象になる）
  - LP はストア公開後に検討（まず README にデモGIF+ストアバッジを足す。Zenn 等の紹介記事のほうが流入を生む想定）

## このプロジェクトでの流儀

- **ネイティブ/ブラウザ標準のUIを尊重**。カスタムスクロールバー等の見た目変更は基本NG（`scrollbar-width: thin`のみ許容）。標準部品が無い場合（トグルスイッチ等）のみ自作を容認。
- **表面的な対症療法ではなく根本原因を説明・修正すること**。CSS/ブラウザ仕様レベルの原因を特定してから直す。ハック（マジックナンバーでの位置調整など）は避ける。
- **ピクセル単位の一貫性にこだわる**: パディング・マージン・角丸・コントロール高さは他要素との整合性（grid列幅と入力幅を揃える等）から値を導出する。
- **UI文言・装飾は最小限に削ぎ落とす**: 冗長なラベルや自明なヒント文、意味の薄い装飾は削除する方向。
- **操作の意味を曖昧にしない**: 婉曲的なON/OFFより明示的な状態表現。意図せず発生する挙動（blur時の自動保存など）は避け、明示的な確定操作を用意する。
- **軽量・低依存を維持**: 外部ライブラリやコンポーネントフレームワークの追加より、必要な部分だけ移植する方針を優先する。
- **セキュリティ/権限のスコープに敏感**: 「このタブでの操作が他タブに影響しないか」を常に意識する。
- **作業フロー**: 提案 → 拡張機能を再読み込みして目視確認（スクリーンショット共有も多い）→ フィードバック → 確認が取れた変更をまとめてコミット（コミット粒度は本人が指定することもある）。
